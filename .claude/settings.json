{
  "env": {
    "CLAUDE_CODE_TASK_LIST_ID": "yesaind"
  },
  "hooks": {
    "PermissionRequest": [
      {
        "hooks": [
          {
            "type": "prompt",
            "prompt": "You are a security reviewer. Evaluate this tool call: $ARGUMENTS\n\nApprove if: standard dev operations (git, npm, file edits, reads, searches, tests).\nDeny if: destructive (rm -rf, force push, drop table), touches credentials/secrets, or reaches outside the project.\n\nRespond with JSON: {\"ok\": true} or {\"ok\": false, \"reason\": \"why\"}",
            "model": "haiku",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
